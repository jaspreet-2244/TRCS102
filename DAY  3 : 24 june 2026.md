## 5. Sets (`set`)
A **set** is a mutable, unordered collection of unique elements.

### Characteristics:
- **Mutable**: Elements can be added or removed, though set elements themselves must be hashable (immutable).
- **Unordered & Unindexed**: Elements have no defined order, and elements cannot be retrieved by index or sliced.
- **Unique Elements**: Duplicate items are automatically removed upon creation or insertion.
- **Mathematical Operations**: Highly optimized for set math like unions, intersections, and differences.

### Common Set Methods:

| Method | Description | Operator Equivalent |
| :--- | :--- | :--- |
| `.add(x)` | Adds element `x` to the set | N/A |
| `.update(iterable)` | Adds all elements from iterable to the set | N/A |
| `.remove(x)` | Removes element `x` (raises `KeyError` if not found) | N/A |
| `.discard(x)` | Removes element `x` if present (does NOT raise error if missing) | N/A |
| `.pop()` | Removes and returns an arbitrary element | N/A |
| `.union(other)` | Returns the union of two sets | `set1 \| set2` |
| `.intersection(other)` | Returns the intersection of two sets | `set1 & set2` |
| `.difference(other)` | Returns elements in the calling set but not in `other` | `set1 - set2` |
| `.symmetric_difference(other)` | Returns elements in either set, but not both | `set1 ^ set2` |
| `.issubset(other)` | Checks if all elements of this set are in `other` | `set1 <= set2` |
| `.issuperset(other)` | Checks if this set contains all elements of `other` | `set1 >= set2` |
| `.isdisjoint(other)` | Checks if two sets have no common elements | N/A |
| `.clear()` | Removes all elements from the set | N/A |

### Built-in Functions with Sets:
- `len(s)`: Returns the cardinality (number of elements) of the set.
- `set(iterable)`: Converts an iterable to a set (often used for deduplication).
- `max(s)` / `min(s)` / `sum(s)`: Standard numerical operations on elements.
- `sorted(s)`: Returns a new *sorted list* containing the set's elements.
## 6. Summary Comparison Table
Here is a quick reference comparing the five data structures:

| Data Structure | Type | Mutability | Ordering | Indexing | Duplicates | Core Syntax | Key Use Case |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **String** | `str` | Immutable | Ordered | Yes (Integer) | Yes | `'text'` or `"text"` | Text data processing |
| **List** | `list` | Mutable | Ordered | Yes (Integer) | Yes | `[item1, item2]` | Ordered collections, stacks, queues |
| **Tuple** | `tuple` | Immutable | Ordered | Yes (Integer) | Yes | `(item1, item2)` | Fixed records, coordinates, dict keys |
| **Dictionary** | `dict` | Mutable | Ordered (3.7+) | Yes (by Key) | Keys: No, Values: Yes | `{'key': 'value'}` | Key-value mapping, lookups |
| **Set** | `set` | Mutable | Unordered | No | No | `{item1, item2}` | Uniqueness, membership checks, set math |
# Practice Questions:
### Sets (`set`)

- **Q5.1: Deduplicating IDs**
  You are given a list with duplicate elements: `user_ids = ["id1", "id2", "id1", "id3", "id2", "id4"]`. Write code to remove all duplicates and print the unique IDs as a list.

- **Q5.2: Interest Matcher**
  Given two sets representing topics of interest:
  `alice_interests = {"reading", "cycling", "cooking"}`
  `bob_interests = {"cooking", "gaming", "cycling"}`
  
  Find and print:
  1. The interests they share in common (intersection).
  2. All unique interests combined across both individuals (union).
  3. The interests Alice has that Bob does not (difference).
  # Answers:
  <img width="719" height="578" alt="Screenshot 2026-06-25 135505" src="https://github.com/user-attachments/assets/1f2af65f-7381-4c20-a4da-6875b31a85ef" />
  
# Operators in Python
 an **operator** is a special symbol that directs the computer to perform specific mathematical, comparison, or logical manipulations on operands (variables and values). For example, in the expression `x + y`, the `+` is the operator and `x` and `y` are the operands.
### Learning Objectives:
In this notebook, we will cover:
1. **Arithmetic Operators**: Performing basic math.
2. **Comparison (Relational) Operators**: Comparing values.
3. **Logical Operators**: Combining conditional statements.
4. **Assignment Operators**: Storing and modifying variables.
5. **Identity Operators (`is`, `is not`)**: Comparing memory locations (objects).
6. **Membership Operators (`in`, `not in`)**: Finding items in sequences and mappings.

> ** Excluded Topics:** Bitwise (binary) and shift operators (`&`, `|`, `^`, `~`, `<<`, `>>`) are specialized tools for low-level memory operations and are skipped in this general-purpose tutorial.
---
##  Arithmetic Operators
Arithmetic operators are used to perform mathematical operations such as addition, subtraction, multiplication, and division.
Here are the 7 arithmetic operators available in Python:
| Operator | Name | Example | Explanation |
| :--- | :--- | :--- | :--- |
| `+` | Addition | `a + b` | Adds two operands |
| `-` | Subtraction | `a - b` | Subtracts the right operand from the left |
| `*` | Multiplication | `a * b` | Multiplies two operands |
| `/` | Division | `a / b` | Divides left operand by right operand (always returns a float) |
| `%` | Modulus | `a % b` | Returns the division remainder |
| `**` | Exponentiation | `a ** b` | Left operand raised to the power of the right (`a` to the power `b`) |
| `//` | Floor Division | `a // b` | Divides and rounds down to the nearest whole number (returns an integer if inputs are integers) |
---
##  Comparison (Relational) Operators
Comparison operators are used to compare two values. They return a Boolean value: either `True` or `False` depending on whether the relationship holds.
| Operator | Name | Example | Explanation |
| :--- | :--- | :--- | :--- |
| `==` | Equal | `x == y` | `True` if operands are equal |
| `!=` | Not Equal | `x != y` | `True` if operands are not equal |
| `>` | Greater Than | `x > y` | `True` if left operand is greater than right |
| `<` | Less Than | `x < y` | `True` if left operand is less than right |
| `>=` | Greater Than or Equal To | `x >= y` | `True` if left operand is greater than or equal to right |
| `<=` | Less Than or Equal To | `x <= y` | `True` if left operand is less than or equal to right |
---
##  Logical Operators
Logical operators are used to combine conditional statements. Python has three logical operators: `and`, `or`, and `not`.

### 1. The `and` Operator
Evaluates to `True` **only if both** operands are `True`.
- `True and True` $\rightarrow$ `True`
- `True and False` $\rightarrow$ `False`
- `False and False` $\rightarrow$ `False`

### 2. The `or` Operator
Evaluates to `True` **if at least one** of the operands is `True`.
- `True or False` $\rightarrow$ `True`
- `False or False` $\rightarrow$ `False`

### 3. The `not` Operator
Reverses the Boolean state of its operand.
- `not True` $\rightarrow$ `False`
- `not False` $\rightarrow$ `True`
---
##  Assignment Operators
Assignment operators are used to assign values to variables. Python also supports **compound assignment operators**, which perform an arithmetic operation and assign the result to the variable in a single step.
| Operator | Example | Equivalent To |
| :--- | :--- | :--- |
| `=` | `x = 5` | `x = 5` |
| `+=` | `x += 3` | `x = x + 3` |
| `-=` | `x -= 3` | `x = x - 3` |
| `*=` | `x *= 3` | `x = x * 3` |
| `/=` | `x /= 3` | `x = x / 3` |
| `%=` | `x %= 3` | `x = x % 3` |
| `//=` | `x //= 3` | `x = x // 3` |
| `**=` | `x **= 3` | `x = x ** 3` |
---
## Identity Operators
Identity operators compare the memory locations (or references) of two objects. In Python, variables are labels that point to objects in memory. 

The two identity operators are:
1. `is`: Returns `True` if both variables point to the exact same object in memory.
2. `is not`: Returns `True` if both variables point to different objects in memory.

###  The Key Difference: `==` vs. `is`
- **`==` (Equality)**: Compares the **values** of the objects (Do they contain the same data?).
- **`is` (Identity)**: Compares the **memory addresses** of the objects (Are they literally the same object in memory?).

To check where an object is stored in memory, Python provides the built-in `id()` function, which returns a unique integer identifier for the object.
---
## Membership Operators
Membership operators are used to test whether a value or variable is found inside a sequence (such as a string, list, tuple, set, or dictionary).

The two membership operators are:
1. `in`: Returns `True` if the specified value is present in the sequence.
2. `not in`: Returns `True` if the specified value is **not** present in the sequence.

### Checking Membership in Different Collections:
- **Strings**: Checks for sub-strings (e.g., `'a'` in `'apple'`).
- **Lists/Tuples/Sets**: Checks for element presence.
- **Dictionaries**: Checks if the item exists **as a key** in the dictionary (not as a value).

> ** Performance Note:** Finding membership in a **List** or **Tuple** requires scanning the collection element-by-element ($O(N)$ linear time). Checking membership in a **Set** or **Dictionary** uses hash tables and is instantaneous ($O(1)$ constant time), regardless of size!
---
# Practice Questions:
### Exercise 1: Leap Year & Even Check (Arithmetic & Comparison)
Write a program that takes a variable `year` (e.g., `year = 2024`) and checks if:
1. It is a leap year (divisible by 4, not divisible by 100, unless also divisible by 400). Hint: Use `%`, `==`, `!=`, and logical operators.
2. It is an even number.
Print appropriate messages for both checks.
## Answer:
<img width="958" height="487" alt="image" src="https://github.com/user-attachments/assets/7188919e-d135-4b18-96ca-14add415ef8b" />


### Exercise 2: Email Domain Checker (Membership)
You have a list of allowed email domains: `allowed_domains = ["gmail.com", "yahoo.com", "outlook.com"]`.
Write code to check if a user's email address (e.g., `"john.doe@gmail.com"`) has an allowed domain. 
*Hint*: You can split the email address to extract the domain, or check if the domain is a substring of the email.
## Answer:
<img width="831" height="438" alt="image" src="https://github.com/user-attachments/assets/cfe86603-50c3-46da-9df1-9dec2f726156" />


### Exercise 3: Value vs. Identity Checker (Identity vs. Equality)
1. Create a variable `x = [10, 20]` and `y = [10, 20]`.
2. Check if they are equal using `==`.
3. Check if they are identical using `is`.
4. Create a third variable `z = x`.
5. Check if `x` and `z` are identical using `is`.
Print all results and a short comment explaining the outcomes.
## Answer:
<img width="679" height="430" alt="image" src="https://github.com/user-attachments/assets/20243db8-4419-425b-8aa1-77cd35dd8423" />
### Explanation of Outcomes:
x == y is True: The == operator compares the values of the two objects. Because both lists contain the exact same elements in the same order, they are considered equal.
x is y is False: The is operator compares the identity (memory address) of the two objects. Because x and y were initialized as two separate list literals, Python allocated two different spots in memory for them. They are independent objects that happen to hold identical data.
x is z is True: The assignment z = x does not create a copy of the list. Instead, it makes z reference the exact same object in memory that x points to. Since they share the same memory location, modifying z (like appending an item) will also modify x.

### Exercise 4: Simple Grade Calculator (Comparison & Logical)
Given two scores: `homework_score = 85` and `exam_score = 72`.
Write a script that decides if a student passes:
* A student passes if both conditions are met:
  1. `homework_score` is greater than or equal to 80.
  2. `exam_score` is greater than or equal to 70.
* Alternatively, if their `exam_score` is a perfect 100, they pass regardless of their homework score.
Print whether the student passed or failed.
## Answer:
<img width="665" height="408" alt="image" src="https://github.com/user-attachments/assets/85b5ed12-ce89-4ff2-b228-039088defae6" />

#  Control Flow Statements
Welcome to this interactive Jupyter Notebook! This guide is designed to help you master Python's **Control Flow Statements**.
Control flow refers to the order in which individual statements, instructions, or function calls of an imperative program are executed. By default, a Python program runs line-by-line from top to bottom. However, control flow statements allow you to bypass this sequential execution, enabling decision-making and repetitive tasks.
In this notebook, we will cover:
1. **Conditional Branching**: `if`, `if-else`, `elif`, and Nested Conditions.
2. **Looping structures**: `for` loops and `while` loops.
3. **Loop Control Statements**: `break`, `continue`, and `pass`.

---
##  Conditional Statements
Conditional statements allow your program to make decisions based on whether a condition is `True` or `False`. They act as decision forks in your code.

We will explore four forms:
1. **Simple `if` statement**
2. **`if-else` statement**
3. **`if-elif-else` statement**
4. **Nested `if` conditions**
###  The `if` Statement
The `if` statement is the simplest form of decision-making. It runs a block of code **only if** the specified condition evaluates to `True`.

#### Syntax:
```python
if condition:
    # Code to run if condition is True
```

> ** Indentation matters!** Python uses 4 spaces (or a tab) of indentation to define the block of code inside the conditional statement. If you forget to indent, Python will throw an `IndentationError`.

#### How it works:
```text
      [Start]
         │
         ▼
  Is condition True?  ───── No ───┐
         │                        │
        Yes                       │
         │                        │
         ▼                        │
   [Run if-block]                 │
         │                        │
         ├────────────────────────┘
         ▼
    [Continue]
```
###  The `if-else` Statement
The `if-else` statement provides an alternative block of code that runs **only if** the `if` condition evaluates to `False`. It's a binary decision maker.

#### Syntax:
```python
if condition:
    # Code to run if condition is True
else:
    # Code to run if condition is False
```

#### How it works:
```text
               [Start]
                  │
                  ▼
         Is condition True?
             /        \
          Yes          No
          /            \
         ▼              ▼
   [Run if-block]   [Run else-block]
         \              /
          \            /
           ▼          ▼
            [Continue]
```
###  The `if-elif-else` Statement (Chained Conditions)
When you have more than two possibilities, you can chain multiple conditions using `elif` (short for **else if**). Python checks each condition from top to bottom and runs the first block that evaluates to `True`. If none of them are true, the final `else` block runs.

#### Syntax:
```python
if condition1:
    # Code for condition1
elif condition2:
    # Code for condition2
elif condition3:
    # Code for condition3
else:
    # Default code if none of the above are True
```
### Nested Conditions
You can place one conditional statement inside another. This is called nesting. It is useful when a decision depends on multiple subsequent conditions.

####  Warning:
Deep nesting can make code hard to read. Where possible, try to simplify nested conditions using logical operators (`and`, `or`).

#### Example Scenario:
Let's check if a candidate is eligible for a loan:
1. They must be at least 18 years old.
2. If they are 18+, we check their monthly income:
   * If income >= 50,000, they are approved for a high limit.
   * If income is less, they are approved for a standard limit.
3. If they are under 18, they are not eligible.
# Practice Question:
### Exercise 1: Number Classifier
Write a program that:
1. Asks the user to enter a number using `input()` and converts it to a float.
2. Uses `if-elif-else` to check if the number is:
   * **Positive** (greater than 0)
   * **Negative** (less than 0)
   * **Zero** (equal to 0)
3. Prints the classification.
# Answer:
<img width="576" height="407" alt="image" src="https://github.com/user-attachments/assets/de883963-a1cb-4c65-ac68-d2d17c379889" />




